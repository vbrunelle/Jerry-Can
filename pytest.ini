[pytest]
markers =
    integration: tests that hit real network endpoints (deselect with: -m "not integration")
    scale: large-scale performance tests requiring a real Hudi table (deselect with: -m "not scale")
